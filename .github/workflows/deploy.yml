name: Build and Deploy

on:
  push:
    branches: [ main ]

env:
  DOCKER_USERNAME: ritikpandey8840
  # We will add these secrets to GitHub later
  # DOCKER_PASSWORD
  # EC2_HOST
  # EC2_SSH_KEY

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ env.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Backend
      uses: docker/build-push-action@v4
      with:
        context: ./backend
        push: true
        tags: ${{ env.DOCKER_USERNAME }}/rag-backend:latest

    - name: Build and push Frontend
      uses: docker/build-push-action@v4
      with:
        context: ./frontend
        push: true
        tags: ${{ env.DOCKER_USERNAME }}/rag-frontend:latest

  # deploy:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: Deploy to EC2
  #     uses: appleboy/ssh-action@master
  #     with:
  #       host: ${{ secrets.EC2_HOST }}
  #       username: ubuntu
  #       key: ${{ secrets.EC2_SSH_KEY }}
  #       script: |
  #         # Commands to run on your EC2 server
  #         docker pull ${{ env.DOCKER_USERNAME }}/rag-backend:latest
  #         docker pull ${{ env.DOCKER_USERNAME }}/rag-frontend:latest
  #         # We will need to copy docker-compose.yml to the server manually once
  #         docker-compose up -d
